# DragonflyDB Environment Configuration
# Copy this file to dragonfly.env and adjust as needed

# Logging
DRAGONFLY_LOG_LEVEL=info

# Data persistence
# Data is persisted to the dragonfly-data volume

# Network
# Port 6499 is exposed for Redis-compatible access (host port)
# Container internal port remains 6379

# No authentication (local only)
# DragonflyDB runs without authentication for local development

# Example usage:
# docker-compose up -d
# redis-cli -h localhost -p 6499

